server:
  port: 8761
  forward-headers-strategy: framework

eureka:
  instance:
    hostname: localhost
    appname: eureka-server-local
  server:
    enable-self-preservation: false
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/